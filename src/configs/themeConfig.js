import { v4 as uuidv4 } from "uuid";
const themeConfig = {
  app: {
    name: "TradingChat React",
  },
  // layout
  layout: {
    isRTL: false,
    darkMode: true,
    semiDarkMode: false,
    skin: "default",
    contentWidth: "full",
    type: "vertical",
    navBarType: "sticky",
    footerType: "static",
    isMonochrome: false,
    isHidden: false,
    mobileMenu: false,
    customizer: false,
  },
};

export default themeConfig;
